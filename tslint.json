{
  "extends": "@oclif/tslint",
  "rules": {
    "trailing-comma": [true, { "multiline": "always", "singleline": "never" }],
    "no-unused": [
      true,
      {
        "ignore-pattern": "^_"
      }
    ]
  }
}
